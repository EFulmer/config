" ~/.vim/ftplugin/python.vim
set tabstop=8     " # of spaces a Tab represents; A Good Idea to keep it 8?
set expandtab     " each hit of Tab becomes the appropriate # of spaces
set shiftwidth=4  " how many spaces to use for each level of indentation
set softtabstop=4 " how many spaces to insert for each Tab while editing
