syntax on
filetype indent plugin on
set modeline
set number " line numbering
set ruler " (line #, column #) in lower right
" for python, code folding (use :za):
set foldmethod=indent
set foldlevel=99
colorscheme zenburn
